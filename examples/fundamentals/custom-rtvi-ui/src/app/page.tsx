export default function HomePage() {
  return (
    <main style={{ padding: 24 }}>
      <h1 style={{ marginTop: 0 }}>Patient Intake Screening Example</h1>
      <p>
        Open <code>/demo</code> to run the workflow template.
      </p>
    </main>
  );
}
